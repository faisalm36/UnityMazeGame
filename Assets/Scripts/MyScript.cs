using UnityEngine;

public class MyScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("MyScript is working!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
